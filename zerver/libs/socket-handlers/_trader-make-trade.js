module.exports = function(people, client) {

  client.on('trader-make-trade', function(user) {
    //TODO
    // 1. if paired, broadcast (actually only send to pair)
    // 2. set to approve by default after timer
  });

};
